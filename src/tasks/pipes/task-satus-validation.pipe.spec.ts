import { TaskSatusValidationPipe } from './task-satus-validation.pipe';

describe('TaskSatusValidationPipe', () => {
  it('should be defined', () => {
    expect(new TaskSatusValidationPipe()).toBeDefined();
  });
});
